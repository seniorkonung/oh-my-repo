# yaml-language-server: $schema=https://raw.githubusercontent.com/espanso/espanso/dev/schemas/match.schema.json

matches:
  - trigger: ':git.dev'
    replace: git checkout main && git branch -D dev && git pull origin main && git checkout -b dev main
